name: Update HackerRank Stats

on:
  schedule:
    # This runs every day at midnight (UTC)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allows to run it manually for testing

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Update HackerRank Stats
        uses: a-p-p-a-s/hackerrank-stats-action@v1 # Versão mantida e funcional
        with:
          username: joaovitor_pcost1
